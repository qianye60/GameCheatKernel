# 王者荣耀游戏安全分析学习文档

> **声明**: 本文档仅用于安全研究和教育目的，旨在帮助理解游戏安全机制和反作弊原理。请勿将相关知识用于违规用途。

---

## 目录

1. [项目架构概述](#1-项目架构概述)
2. [内核级内存读写原理](#2-内核级内存读写原理)
3. [用户态内存读写原理](#3-用户态内存读写原理)
4. [游戏数据结构分析](#4-游戏数据结构分析)
5. [坐标系统与矩阵变换](#5-坐标系统与矩阵变换)
6. [自动瞄准算法原理](#6-自动瞄准算法原理)
7. [触摸模拟系统](#7-触摸模拟系统)
8. [图形渲染系统](#8-图形渲染系统)
9. [Android原生窗口机制](#9-android原生窗口机制)
10. [游戏反检测机制](#10-游戏反检测机制)
11. [关键C/C++语法详解](#11-关键cc语法详解)

---

## 1. 项目架构概述

### 1.1 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                    用户界面层 (UI Layer)                      │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │   ImGui框架  │  │  悬浮窗控件  │  │   配置管理系统       │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                   游戏逻辑层 (Game Logic)                    │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │  自动瞄准引擎 │  │  目标检测器  │  │   坐标转换系统       │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                  系统交互层 (System Interface)               │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │  触摸模拟器  │  │  渲染管线    │  │   进程通信           │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                    内存访问层 (Memory Access)                │
│  ┌─────────────────────────┐  ┌─────────────────────────┐  │
│  │   syscall直接调用方式     │  │   内核驱动ioctl方式     │  │
│  │   (process_vm_readv)    │  │   (自定义驱动模块)       │  │
│  └─────────────────────────┘  └─────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                    Linux内核层 (Kernel)                      │
│  ┌─────────────────────────────────────────────────────┐   │
│  │   目标游戏进程内存空间 (com.tencent.tmgp.sgame)       │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 核心文件说明

| 文件名 | 功能描述 |
|--------|----------|
| `内核读写.h` | 内核驱动方式的内存读写封装 |
| `sys读写.h` | syscall方式的内存读写实现 |
| `触摸自瞄.hpp` | 自动瞄准核心算法 |
| `源码库.hpp` | EGL初始化和屏幕配置 |
| `绘图.cpp` | 主渲染循环和游戏数据读取 |
| `TouchHelperA.h` | 触摸事件模拟接口 |
| `ANativeWindowCreator.h` | Android原生窗口创建 |

---

## 2. 内核级内存读写原理

### 2.1 什么是内核级内存读写

在Linux系统中，每个进程都有独立的虚拟地址空间，进程之间无法直接访问彼此的内存。但通过内核模块（驱动），可以绕过这种限制，直接访问任意进程的内存。

### 2.2 驱动通信机制：ioctl

`ioctl`（Input/Output Control）是Linux中用于设备控制的系统调用，用户态程序通过它与内核驱动通信。

#### 2.2.1 ioctl基本语法

```c
#include <sys/ioctl.h>

int ioctl(int fd, unsigned long request, ...);
```

**参数说明**：
- `fd`: 设备文件描述符，通过`open()`打开
- `request`: 操作命令码，自定义的请求类型
- `...`: 可选参数，通常是指向数据结构的指针

#### 2.2.2 代码解析：内核读写类

```cpp
// 文件: 内核读写.h

class c_driver {
private:
    int fd;           // 驱动文件描述符
    pid_t 进程;       // 目标进程ID

    // 内存操作结构体 - 用于与驱动通信
    typedef struct _COPY_MEMORY {
        pid_t 进程;        // 目标进程ID
        uintptr_t 地址;    // 要读写的内存地址
        void *buffer;      // 数据缓冲区
        size_t size;       // 数据大小
    } COPY_MEMORY, *PCOPY_MEMORY;

    // 模块基址结构体
    typedef struct _MODULE_BASE {
        pid_t 进程;
        char *name;        // 模块名称（如 "libil2cpp.so"）
        uintptr_t 基址头;   // 模块加载基址
        uintptr_t 基址头2;
    } MODULE_BASE, *PMODULE_BASE;

    // 操作命令码枚举
    enum OPERATIONS {
        OP_INIT_KEY = 0x800,     // 初始化
        OP_READ_MEM = 0x801,     // 读内存
        OP_WRITE_MEM = 0x802,    // 写内存
        OP_MODULE_BASE = 0x803,  // 获取模块基址
    };
```

**知识点解析**：

1. **`typedef struct`** - C语言中定义结构体类型的方式
   ```c
   typedef struct _COPY_MEMORY {
       // 成员...
   } COPY_MEMORY, *PCOPY_MEMORY;
   // COPY_MEMORY 是结构体类型名
   // PCOPY_MEMORY 是指向该结构体的指针类型 (等同于 COPY_MEMORY*)
   ```

2. **`uintptr_t`** - 无符号整数类型，大小与指针相同
   - 32位系统：4字节
   - 64位系统：8字节
   - 用于存储内存地址

3. **`enum`** - 枚举类型，定义一组命名的整数常量
   ```c
   enum OPERATIONS {
       OP_INIT_KEY = 0x800,   // 值为 2048
       OP_READ_MEM = 0x801,   // 值为 2049
       // ...
   };
   ```

#### 2.2.3 内存读取实现

```cpp
bool 读取(uintptr_t 地址, void *buffer, size_t size) {
    // 地址有效性检查
    if (地址 <= 0x10000000 || 地址 % 4 != 0 || 地址 >= 0x10000000000) {
        return 0;
    }

    // 构造通信结构体
    COPY_MEMORY cm;
    cm.进程 = this->进程;
    cm.地址 = 地址;
    cm.buffer = buffer;
    cm.size = size;

    // 通过ioctl与驱动通信
    if (ioctl(fd, OP_READ_MEM, &cm) != 0) {
        return false;
    }
    return true;
}
```

**地址校验逻辑解析**：
```cpp
if (地址 <= 0x10000000 || 地址 % 4 != 0 || 地址 >= 0x10000000000)
```
- `地址 <= 0x10000000`: 排除低地址区域（通常是系统保留区）
- `地址 % 4 != 0`: 要求4字节对齐（ARM架构要求）
- `地址 >= 0x10000000000`: 排除超出有效范围的地址

#### 2.2.4 模板函数：类型安全的读取

```cpp
template <typename T>
T 读取(uintptr_t 地址) {
    T res;
    if (this->读取(地址, &res, sizeof(T)))
        return res;
    return {};  // 返回默认值
}
```

**模板语法详解**：
- `template <typename T>` - 声明这是一个模板函数，T是类型参数
- `sizeof(T)` - 获取类型T的字节大小
- `return {}` - C++11的列表初始化，返回类型的默认值

**使用示例**：
```cpp
// 读取int类型（4字节）
int health = 驱动->读取<int>(地址);

// 读取float类型（4字节）
float posX = 驱动->读取<float>(地址);

// 读取long类型（8字节）
long pointer = 驱动->读取<long>(地址);
```

#### 2.2.5 便捷读取函数

```cpp
// 读取指针（64位游戏）
long 读取指针(long 地址) {
    if (地址 <= 0x10000000 || 地址 % 4 != 0 || 地址 >= 0x10000000000) {
        return 0;
    }
    long res;
    if (this->读取(地址, &res, sizeof(uintptr_t))) {
        return res;
    }
    return {};
}

// 读取浮点数
float 读取浮点数(long 地址) {
    if (地址 < 0xFFFFFF) {
        return 0;
    }
    float var;
    if (this->读取(地址 & 0xFFFFFFFFFF, &var, sizeof(var))) {
        return var;
    }
    return {};
}
```

**位运算解析**：
```cpp
地址 & 0xFFFFFFFFFF
```
- `&` 是按位与运算
- `0xFFFFFFFFFF` 是40位全1的掩码
- 目的是截取地址的低40位，确保地址在有效范围内

---

## 3. 用户态内存读写原理

### 3.1 process_vm_readv/writev 系统调用

这是Linux提供的跨进程内存访问系统调用，无需内核驱动即可使用。

#### 3.1.1 系统调用号定义

```cpp
// 文件: sys读写.h

// 根据CPU架构定义不同的系统调用号
#if defined(__arm__)
int process_vm_readv_syscall = 376;
int process_vm_writev_syscall = 377;
#elif defined(__aarch64__)
int process_vm_readv_syscall = 270;
int process_vm_writev_syscall = 271;
#elif defined(__i386__)
int process_vm_readv_syscall = 347;
int process_vm_writev_syscall = 348;
#else
int process_vm_readv_syscall = 310;
int process_vm_writev_syscall = 311;
#endif
```

**预处理器指令解析**：
- `#if defined(__arm__)` - 检查是否为ARM 32位架构
- `#elif defined(__aarch64__)` - ARM 64位架构
- `#elif defined(__i386__)` - x86 32位架构
- `#else` - 其他架构（通常是x86_64）

#### 3.1.2 iovec结构体

```cpp
#include <sys/uio.h>

struct iovec {
    void  *iov_base;  // 缓冲区起始地址
    size_t iov_len;   // 缓冲区长度
};
```

`iovec` 是"分散/聚集I/O"的核心结构，允许一次系统调用操作多个不连续的内存区域。

#### 3.1.3 内存读写封装

```cpp
// 底层系统调用封装
ssize_t process_v(pid_t __pid,
                  const struct iovec *__local_iov,
                  unsigned long __local_iov_count,
                  const struct iovec *__remote_iov,
                  unsigned long __remote_iov_count,
                  unsigned long __flags,
                  bool iswrite) {
    return syscall(
        (iswrite ? process_vm_writev_syscall : process_vm_readv_syscall),
        __pid,
        __local_iov, __local_iov_count,
        __remote_iov, __remote_iov_count,
        __flags
    );
}

// 进程内存读写
bool pvm(void *address, void *buffer, size_t size, bool iswrite) {
    struct iovec local[1];
    struct iovec remote[1];

    // 本地缓冲区（本进程）
    local[0].iov_base = buffer;
    local[0].iov_len = size;

    // 远程地址（目标进程）
    remote[0].iov_base = address;
    remote[0].iov_len = size;

    if (pid < 0) {
        return false;
    }

    ssize_t bytes = process_v(pid, local, 1, remote, 1, 0, iswrite);
    return bytes == size;
}

// 读取内存的便捷函数
bool vm_readv(unsigned long address, void *buffer, size_t size) {
    return pvm(reinterpret_cast<void*>(address), buffer, size, false);
}

// 写入内存的便捷函数
bool vm_writev(unsigned long address, void *buffer, size_t size) {
    return pvm(reinterpret_cast<void*>(address), buffer, size, true);
}
```

**C++类型转换**：
```cpp
reinterpret_cast<void*>(address)
```
- `reinterpret_cast` - C++强制类型转换，用于指针和整数之间的转换
- 将整数地址转换为void指针类型

#### 3.1.4 获取进程ID

```cpp
int getProcessID(const char *packageName) {
    int id = -1;
    DIR *dir;
    FILE *fp;
    char filename[64];
    char cmdline[64];
    struct dirent *entry;

    // 打开 /proc 目录
    dir = opendir("/proc");

    // 遍历所有进程
    while ((entry = readdir(dir)) != NULL) {
        id = atoi(entry->d_name);  // 将目录名转为数字（PID）
        if (id != 0) {
            // 读取进程的命令行
            sprintf(filename, "/proc/%d/cmdline", id);
            fp = fopen(filename, "r");
            if (fp) {
                fgets(cmdline, sizeof(cmdline), fp);
                fclose(fp);
                // 比较包名
                if (strcmp(packageName, cmdline) == 0) {
                    return id;
                }
            }
        }
    }
    closedir(dir);
    return -1;
}
```

**Linux /proc 文件系统**：
- `/proc` 是虚拟文件系统，包含系统和进程信息
- `/proc/[pid]/cmdline` - 进程的启动命令行参数
- `/proc/[pid]/maps` - 进程的内存映射

#### 3.1.5 获取模块基址

```cpp
unsigned long get_module_base(int pid, const char *module_name) {
    FILE *fp;
    unsigned long addr = 0;
    char *pch;
    char filename[64];
    char line[1024];

    // 构造maps文件路径
    snprintf(filename, sizeof(filename), "/proc/%d/maps", pid);
    fp = fopen(filename, "r");

    if (fp != NULL) {
        // 逐行读取
        while (fgets(line, sizeof(line), fp)) {
            // 搜索模块名
            if (strstr(line, module_name)) {
                // 解析起始地址
                pch = strtok(line, "-");
                addr = strtoul(pch, NULL, 16);
                if (addr == 0x8000)
                    addr = 0;
                break;
            }
        }
        fclose(fp);
    }
    return addr;
}
```

**/proc/[pid]/maps 格式示例**：
```
地址范围                权限 偏移    设备   节点  路径名
7f7b8c000000-7f7b8c400000 rw-p 00000000 00:00 0    [anon:libil2cpp.so]
7f7b8d000000-7f7b8e200000 r-xp 00000000 fd:01 1234 /data/app/.../lib/arm64/libil2cpp.so
```

**字符串解析函数**：
- `strstr(line, module_name)` - 在line中查找module_name
- `strtok(line, "-")` - 用"-"分割字符串
- `strtoul(pch, NULL, 16)` - 将16进制字符串转为无符号长整数

---

## 4. 游戏数据结构分析

### 4.1 王者荣耀内存结构

#### 4.1.1 基础数据类型定义

```cpp
// 三维向量结构体
struct Vector3A {
    float X, Y, Z;
};

// 二维向量结构体
struct Vector2A {
    float X, Y;
};

// 屏幕配置结构体
struct Screen {
    int ScreenX;  // 屏幕宽度
    int ScreenY;  // 屏幕高度
} full_screen;
```

#### 4.1.2 游戏对象指针链

王者荣耀使用Unity引擎（IL2CPP），数据存储在层层嵌套的指针中：

```cpp
// 获取矩阵地址的指针链
矩阵 = 读取指针(
    读取指针(
        读取指针(
            读取指针(
                读取指针(基址头 + 0xB939688) + 0x480
            ) + 0xA0
        ) + 0x10
    ) + 0x10
) + 0x310;

// 获取对象数组
数组 = 读取指针(
    读取指针(
        读取指针(
            读取指针(
                读取指针(
                    读取指针(基址头 + 0xBAC29C8) + 0xA0
                ) + 0x0
            ) + 0x30
        ) + 0x28
    ) + 0x70
) + 0x10;
```

**指针链图解**：
```
基址头 (libil2cpp.so的加载地址)
    │
    ├──+ 0xB939688 ──→ [指针1]
    │                      │
    │                      └──+ 0x480 ──→ [指针2]
    │                                        │
    │                                        └──+ 0xA0 ──→ [指针3]
    │                                                        │
    │                                                        └──+ 0x10 ──→ [指针4]
    │                                                                        │
    │                                                                        └──+ 0x10 ──→ [指针5]
    │                                                                                        │
    │                                                                                        └──+ 0x310 ──→ 矩阵数据
```

#### 4.1.3 玩家对象结构

```cpp
// 遍历游戏中的玩家
for (int i = 0; i < 15; i++) {
    // 获取玩家对象指针
    对象指针 = 读取指针(对象阵列 + 8 * i);

    // 读取玩家属性
    int 对象类型 = 读取整数(读取指针(对象指针 + 0x420) + 0x40);

    // 判断是否是自身
    if (对象类型 == 1) {
        自身对象 = 对象指针;
    }

    // 读取坐标组件
    long int 坐标组件 = 读取指针(
        读取指针(
            读取指针(对象指针 + 0x3F8) + 0x20
        ) + 0x10
    );

    // 读取3D坐标
    Vector3A 坐标;
    vm_readv(坐标组件 + 0x324, &坐标, sizeof(坐标));

    // 读取阵营ID
    int 阵营ID = 读取整数(读取指针(对象指针 + 0x420) + 0x100);

    // 读取血量
    float 血量 = 读取浮点数(对象指针 + 0x68);
}
```

**偏移量说明表**：

| 偏移 | 类型 | 含义 |
|------|------|------|
| +0x420 | 指针 | 玩家信息组件 |
| +0x3F8 | 指针 | 坐标组件链头 |
| +0x324 | Vector3 | 世界坐标 |
| +0x68 | float | 当前血量 |
| +0x40 | int | 对象类型标识 |
| +0x100 | int | 阵营ID |
| +0x428 | 指针 | 玩家名字组件 |

---

## 5. 坐标系统与矩阵变换

### 5.1 什么是视图投影矩阵

游戏中的3D物体需要投影到2D屏幕上显示，这个转换过程使用一个4x4矩阵完成。

#### 5.1.1 读取矩阵数据

```cpp
float matrix[16];  // 4x4矩阵

// 从游戏内存读取矩阵
memset(matrix, 0, 16);  // 清零
vm_readv(矩阵, matrix, 16 * 4);  // 读取64字节（16个float）
```

**矩阵结构**：
```
matrix[0]  matrix[4]  matrix[8]   matrix[12]
matrix[1]  matrix[5]  matrix[9]   matrix[13]
matrix[2]  matrix[6]  matrix[10]  matrix[14]
matrix[3]  matrix[7]  matrix[11]  matrix[15]
```

### 5.2 世界坐标到屏幕坐标转换

#### 5.2.1 WorldToScreen函数原理

```cpp
Vector2A WorldToScreen(Vector3A 敌人, float* matrix, float camera) {
    Vector2A out;

    // 透视除法
    out.X = px + (matrix[0] * 敌人.X + matrix[4] * 敌人.Z +
                  matrix[8] * 敌人.Y + matrix[12]) / camera * px;

    out.Y = py - (matrix[1] * 敌人.X + matrix[5] * 敌人.Z +
                  matrix[9] * 敌人.Y + matrix[13]) / camera * py;

    return out;
}
```

#### 5.2.2 转换步骤详解

**步骤1：计算相机深度（W分量）**
```cpp
float camera = matrix[3] * 敌人.X +
               matrix[7] * 敌人.Y +
               matrix[11] * 敌人.Z +
               matrix[15];
```
- 这是透视投影的关键，表示物体到相机的距离
- 用于后续的透视除法

**步骤2：计算屏幕X坐标**
```cpp
r_x = px + (matrix[0] * 敌人.X +
            matrix[4] * 敌人.Z +
            matrix[8] * 敌人.Y +
            matrix[12]) / camera * px;
```
- `px` 是屏幕中心X坐标（screen_x / 2）
- 除以camera实现透视效果（远处物体显示更小）

**步骤3：计算屏幕Y坐标**
```cpp
r_y = py - (matrix[1] * 敌人.X +
            matrix[5] * (敌人.Z + 0.5) +  // 加0.5调整人物中心
            matrix[9] * 敌人.Y +
            matrix[13]) / camera * py;
```
- 注意Y轴使用减法，因为屏幕坐标Y轴向下为正

**步骤4：计算方框高度**
```cpp
// 计算人物头顶位置
r_w = py - (matrix[1] * 敌人.X +
            matrix[5] * (敌人.Z + 1.5) +  // 1.5是人物高度
            matrix[9] * 敌人.Y +
            matrix[13]) / camera * py;

// 方框宽高计算
W = (r_y - r_w) / 2;
X = r_x - W / 4;
Y = r_y;
```

### 5.3 距离计算

```cpp
// 欧几里得距离公式
double 距离 = sqrt(
    pow(敌人.X - 自身.X, 2) +
    pow(敌人.Z - 自身.Z, 2) +
    pow(敌人.Y - 自身.Y, 2)
);

// 屏幕距离（到准心的像素距离）
float 准心距离 = sqrt(
    pow(r_x - px, 2) +
    pow(r_y - py, 2)
);
```

**数学函数说明**：
- `sqrt(x)` - 平方根
- `pow(x, 2)` - x的平方

---

## 6. 自动瞄准算法原理

### 6.1 目标选择算法

```cpp
// 文件: 触摸自瞄.hpp

struct AimStruct {
    Vector3A 瞄准目标;      // 目标3D坐标
    Vector3A 自身目标;      // 自身3D坐标
    float 准心最近距离 = 0; // 到屏幕准心的像素距离
    float 判断距离 = 0;     // 游戏内3D距离
} 瞄准[100];  // 最多100个目标

// 获取最近目标
int 获取目标() {
    float 范围 = 300;     // 初始范围阈值
    int 范围2 = 999;      // 目标索引

    for (int i = 0; i < 全部数量; i++) {
        // 找到距离准心最近的目标
        if (瞄准[i].准心最近距离 < 范围 && 瞄准[i].准心最近距离 != 0) {
            范围 = 瞄准[i].准心最近距离;
            范围2 = i;
        }
    }

    if (范围2 == 999) {
        最近范围 = -1;
        return -1;  // 没有找到目标
    }

    最近范围 = 范围2;
    共量距离 = 瞄准[范围2].判断距离;
    return 范围2;
}
```

**算法逻辑**：
1. 遍历所有潜在目标
2. 选择距离屏幕准心最近的敌人
3. 排除距离为0的无效目标

### 6.2 自动瞄准主循环

```cpp
void AimBotAuto(ImDrawList *Draw) {
    bool 触摸开启 = false;
    double 瞄准点X = NumIo[5], 瞄准点Y = NumIo[6];  // 初始触摸位置
    double 分辨率X = screen_x, 分辨率Y = screen_y;
    double 一半屏幕X = 分辨率X / 2.0f;
    double 一半屏幕Y = 分辨率Y / 2.0f;

    static float 触摸目标X = 0;
    static float 触摸目标Y = 0;

    Touch_Init(&screen_x, &screen_y);  // 初始化触摸

    while (1) {
        // 检查开关状态
        if (!DrawIo[2]) {  // 自瞄未开启
            if (触摸开启) {
                Touch_Up(8);  // 释放触摸
                触摸开启 = false;
            }
            usleep(NumIo[9] * 1000);  // 休眠
            continue;
        }

        获取目标();

        if (最近范围 == -1) {  // 无目标
            if (触摸开启) {
                Touch_Up(8);
                触摸开启 = false;
            }
            usleep(NumIo[9] * 1000);
            continue;
        }

        // ... 瞄准逻辑
    }
}
```

**`static` 变量解释**：
```cpp
static float 触摸目标X = 0;
```
- `static`使变量在函数调用之间保持值
- 只初始化一次，后续调用保留上次的值

**`usleep` 函数**：
```cpp
usleep(NumIo[9] * 1000);
// usleep参数单位是微秒（1/1000000秒）
// 如果NumIo[9]=10，则休眠10毫秒
```

### 6.3 自适应速度算法

```cpp
// 计算到准心的距离
float AimDs = sqrt(pow(px - 预瞄点.X, 2) + pow(py - 预瞄点.Y, 2));

// 根据距离调整瞄准速度
float 自瞄速度 = NumIo[4];  // 基础速度

if (AimDs < 1)
    自瞄速度 = NumIo[4] / 0.09;      // 非常近：速度最快
else if (AimDs < 2)
    自瞄速度 = NumIo[4] / 0.11;
else if (AimDs < 3)
    自瞄速度 = NumIo[4] / 0.12;
else if (AimDs < 5)
    自瞄速度 = NumIo[4] / 0.15;
else if (AimDs < 10)
    自瞄速度 = NumIo[4] / 0.25;
else if (AimDs < 15)
    自瞄速度 = NumIo[4] / 0.4;
else if (AimDs < 20)
    自瞄速度 = NumIo[4] / 0.5;
// ... 更多阶梯
else
    自瞄速度 = NumIo[4] / 1.55;      // 很远：速度最慢
```

**为什么要自适应速度**：
- 目标离准心近时：需要更精细的移动，所以速度要快（除以小数=增大）
- 目标离准心远时：需要大幅移动，所以速度要慢（除以大数=减小）
- 这样可以实现平滑的追踪效果

### 6.4 触摸偏移计算

```cpp
// 计算水平偏移
if (自瞄X > 一半屏幕X) {
    触摸目标X = -(一半屏幕X - 自瞄X);  // 目标在右边
    触摸目标X /= 自瞄速度;

    // 边界检查
    if (触摸目标X + 一半屏幕X > 一半屏幕X * 2)
        触摸目标X = 0;
}

if (自瞄X < 一半屏幕X) {
    触摸目标X = 自瞄X - 一半屏幕X;     // 目标在左边
    触摸目标X /= 自瞄速度;

    if (触摸目标X + 一半屏幕X < 0)
        触摸目标X = 0;
}

// 垂直方向类似...

// 防抖动：偏移过大时重置
if (触摸目标Y >= 35 || 触摸目标X >= 35 ||
    触摸目标Y <= -35 || 触摸目标X <= -35) {
    Touch_Up(8);
    触摸开启 = false;
    continue;
}

// 应用偏移
瞄准点X += 触摸目标X;
瞄准点Y += 触摸目标Y;

// 执行触摸移动
Touch_Move(8, (int)瞄准点X, (int)瞄准点Y);
```

---

## 7. 触摸模拟系统

### 7.1 Linux输入子系统

Android的触摸事件通过Linux输入子系统处理，设备文件位于 `/dev/input/`。

#### 7.1.1 触摸设备结构

```cpp
// 文件: TouchHelperA.h

namespace Touch {
    // 单个触摸点
    struct touchObj {
        Vector2 pos{};      // 触摸位置
        int id = 0;         // 触摸点ID（多点触控）
        bool isDown = false; // 是否按下
    };

    // 触摸设备
    struct Device {
        int fd;             // 设备文件描述符
        float S2TX;         // 屏幕到触摸的X缩放
        float S2TY;         // 屏幕到触摸的Y缩放
        input_absinfo absX, absY;  // 触摸板参数
        touchObj Finger[10]; // 支持10点触控

        Device() {
            memset((void*)this, 0, sizeof(*this));
        }
    };
}
```

#### 7.1.2 Linux输入事件结构

```cpp
#include <linux/input.h>

struct input_event {
    struct timeval time;  // 事件时间
    __u16 type;          // 事件类型
    __u16 code;          // 事件代码
    __s32 value;         // 事件值
};
```

**事件类型（type）**：
- `EV_SYN (0x00)` - 同步事件
- `EV_KEY (0x01)` - 按键事件
- `EV_ABS (0x03)` - 绝对坐标事件

**触摸事件代码（code）**：
- `ABS_MT_POSITION_X` - X坐标
- `ABS_MT_POSITION_Y` - Y坐标
- `ABS_MT_TRACKING_ID` - 触摸点追踪ID
- `ABS_MT_SLOT` - 多点触控槽位

#### 7.1.3 触摸接口函数

```cpp
namespace Touch {
    // 初始化触摸设备
    bool Init(const Vector2 &s, bool p_readOnly);

    // 按下
    void Down(float x, float y);

    // 移动
    void Move(float x, float y);

    // 抬起
    void Up();

    // 上传事件到内核
    void Upload();

    // 关闭设备
    void Close();

    // 设置屏幕方向
    void setOrientation(int orientation);
}
```

### 7.2 触摸事件模拟流程

```
1. 打开触摸设备 (/dev/input/eventX)
       ↓
2. 获取设备参数 (ioctl EVIOCGABS)
       ↓
3. 计算坐标缩放比 (屏幕尺寸/触摸板尺寸)
       ↓
4. 构造input_event结构体
       ↓
5. 写入设备文件 (write)
       ↓
6. 发送同步事件 (EV_SYN)
```

---

## 8. 图形渲染系统

### 8.1 EGL初始化

EGL（Embedded-System Graphics Library）是OpenGL ES与原生窗口系统之间的接口。

```cpp
// 文件: 源码库.hpp

int init_egl(int _screen_x, int _screen_y, bool log) {
    // 1. 创建原生窗口
    native_window = android::ANativeWindowCreator::Create(
        "泪心",       // 窗口名称
        _screen_x,    // 宽度
        _screen_y,    // 高度
        false         // skipScreenshot
    );
    ANativeWindow_acquire(native_window);

    // 2. 获取EGL显示连接
    display = eglGetDisplay(EGL_DEFAULT_DISPLAY);
    if (display == EGL_NO_DISPLAY) {
        printf("eglGetDisplay error=%u\n", glGetError());
        return -1;
    }

    // 3. 初始化EGL
    if (eglInitialize(display, 0, 0) != EGL_TRUE) {
        printf("eglInitialize error=%u\n", glGetError());
        return -1;
    }

    // 4. 配置属性
    const EGLint attribList[] = {
        EGL_SURFACE_TYPE, EGL_WINDOW_BIT,
        EGL_RENDERABLE_TYPE, EGL_OPENGL_ES2_BIT,
        EGL_BLUE_SIZE, 5,
        EGL_GREEN_SIZE, 6,
        EGL_RED_SIZE, 5,
        EGL_BUFFER_SIZE, 32,
        EGL_DEPTH_SIZE, 16,
        EGL_STENCIL_SIZE, 8,
        EGL_NONE  // 数组结束标记
    };

    // 5. 选择配置
    EGLint num_config = 0;
    eglChooseConfig(display, attribList, &config, 1, &num_config);

    // 6. 创建OpenGL ES 3.0上下文
    const EGLint attrib_list[] = {
        EGL_CONTEXT_CLIENT_VERSION, 3,  // OpenGL ES 3.0
        EGL_NONE
    };
    context = eglCreateContext(display, config, EGL_NO_CONTEXT, attrib_list);

    // 7. 创建窗口表面
    surface = eglCreateWindowSurface(display, config, native_window, nullptr);

    // 8. 绑定上下文
    eglMakeCurrent(display, surface, surface, context);

    return 1;
}
```

### 8.2 屏幕方向检测

```cpp
void screen_config() {
    // 获取屏幕尺寸
    std::string window_size = exec("wm size");
    sscanf(window_size.c_str(), "Physical size: %dx%d", &screen_x, &screen_y);

    full_screen.ScreenX = screen_x;
    full_screen.ScreenY = screen_y;

    // 启动方向检测线程
    std::thread *orithread = new std::thread([&] {
        while (true) {
            // 获取当前屏幕方向
            Orientation = atoi(exec(
                "dumpsys display | grep 'mCurrentOrientation' | cut -d'=' -f2"
            ).c_str());

            // 根据方向调整宽高
            if (Orientation == 0 || Orientation == 2) {  // 竖屏
                screen_x = full_screen.ScreenX;
                screen_y = full_screen.ScreenY;
            }
            if (Orientation == 1 || Orientation == 3) {  // 横屏
                screen_x = full_screen.ScreenY;
                screen_y = full_screen.ScreenX;
            }

            std::this_thread::sleep_for(0.5s);
        }
    });
    orithread->detach();  // 分离线程
}
```

**屏幕方向值**：
- 0: Portrait (竖屏正向)
- 1: Landscape (横屏)
- 2: Portrait反向
- 3: Landscape反向

### 8.3 ImGui渲染

ImGui（Immediate Mode GUI）是一个即时模式GUI库。

```cpp
// 绘制方框
void DrawRect(float left, float top, float right, float bottom,
              ImColor color, float rounding, float thickness) {
    ImGui::GetForegroundDrawList()->AddRect(
        {left, top},
        {right, bottom},
        color,
        rounding,
        0,
        thickness
    );
}

// 绘制填充矩形
void DrawRectFilled(float x1, float y1, float x2, float y2,
                    float r, float g, float b, float a) {
    ImGui::GetForegroundDrawList()->AddRectFilled(
        {x1, y1},
        {x2, y2},
        ImColor(r, g, b, a)
    );
}

// 绘制线段
void DrawLine(float x1, float y1, float x2, float y2,
              float r, float g, float b, float a, float thickness) {
    ImGui::GetForegroundDrawList()->AddLine(
        {x1, y1},
        {x2, y2},
        ImColor(r, g, b, a),
        thickness
    );
}

// 绘制带描边的文字
void 绘制字体描边(float size, float x, float y, ImColor color, const char* text) {
    // 先画黑色描边
    ImGui::GetForegroundDrawList()->AddText(NULL, size, {x-1, y}, 黑色, text);
    ImGui::GetForegroundDrawList()->AddText(NULL, size, {x+1, y}, 黑色, text);
    ImGui::GetForegroundDrawList()->AddText(NULL, size, {x, y-1}, 黑色, text);
    ImGui::GetForegroundDrawList()->AddText(NULL, size, {x, y+1}, 黑色, text);
    // 再画彩色文字
    ImGui::GetForegroundDrawList()->AddText(NULL, size, {x, y}, color, text);
}
```

---

## 9. Android原生窗口机制

### 9.1 SurfaceFlinger架构

Android使用SurfaceFlinger服务管理所有屏幕绘制。

```
┌──────────────────────────────────────────────────┐
│                   应用进程                        │
│  ┌────────────────────────────────────────────┐ │
│  │        ANativeWindow (原生窗口)             │ │
│  └────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────┘
                         │
                         ↓ Binder IPC
┌──────────────────────────────────────────────────┐
│               SurfaceFlinger服务                  │
│  ┌────────────────────────────────────────────┐ │
│  │     SurfaceComposerClient (客户端接口)      │ │
│  │              ↓                              │ │
│  │     SurfaceControl (Surface控制器)          │ │
│  │              ↓                              │ │
│  │     Layer (图层管理)                        │ │
│  └────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────┘
                         │
                         ↓
┌──────────────────────────────────────────────────┐
│                  Hardware (GPU/显示器)            │
└──────────────────────────────────────────────────┘
```

### 9.2 ANativeWindowCreator解析

```cpp
// 文件: ANativeWindowCreator.h

namespace android {
    class ANativeWindowCreator {
    public:
        // 创建原生窗口
        static ANativeWindow* Create(
            const char *name,       // 窗口名称
            int32_t width = -1,     // 宽度（-1自动获取）
            int32_t height = -1,    // 高度
            bool skipScreenshot = false  // 是否排除截图
        ) {
            auto &surfaceComposerClient = GetComposerInstance();

            // 如果未指定尺寸，获取显示器信息
            while (-1 == width || -1 == height) {
                detail::ui::DisplayState displayInfo{};
                if (!surfaceComposerClient.GetDisplayInfo(&displayInfo))
                    break;
                width = displayInfo.layerStackSpaceRect.width;
                height = displayInfo.layerStackSpaceRect.height;
                break;
            }

            // 创建Surface
            auto surfaceControl = surfaceComposerClient.CreateSurface(
                name, width, height, skipScreenshot
            );

            // 获取ANativeWindow
            auto nativeWindow = reinterpret_cast<ANativeWindow*>(
                surfaceControl.GetSurface()
            );

            // 缓存SurfaceControl
            m_cachedSurfaceControl.emplace(nativeWindow, std::move(surfaceControl));

            return nativeWindow;
        }
    };
}
```

### 9.3 Android版本适配

项目支持Android 9-15，需要处理不同版本的API差异：

```cpp
// 版本判断和符号解析
static std::unordered_map<size_t, std::unordered_map<void**, const char*>> patchesTable = {
    {
        15,  // Android 15
        {
            {reinterpret_cast<void**>(&LayerMetadata__Constructor),
             "_ZN7android3gui13LayerMetadataC2Ev"},
            {reinterpret_cast<void**>(&SurfaceComposerClient__CreateSurface),
             "_ZN7android21SurfaceComposerClient13createSurfaceERKNS_7String8EjjiiRKNS_2spINS_7IBinderEEENS_3gui13LayerMetadataEPj"},
        },
    },
    {
        14,  // Android 14
        // ... 不同的符号
    },
    {
        9,   // Android 9
        {
            {reinterpret_cast<void**>(&SurfaceComposerClient__CreateSurface_and9),
             "_ZN7android21SurfaceComposerClient13createSurfaceERKNS_7String8EjjijPNS_14SurfaceControlEii"},
            // ...
        },
    },
};
```

**C++名称修饰（Name Mangling）**：
```
_ZN7android21SurfaceComposerClient13createSurfaceERKNS_7String8EjjijRKNS_2spINS_7IBinderEEENS_13LayerMetadataEPj
```
解析：
- `_Z` - C++符号前缀
- `N` - 嵌套名称开始
- `7android` - 命名空间"android"（长度7）
- `21SurfaceComposerClient` - 类名（长度21）
- `13createSurface` - 方法名（长度13）
- `E` - 嵌套名称结束
- 后面是参数类型编码

---

## 10. 游戏反检测机制

### 10.1 缓存清理

```cpp
// 文件: include.h

void delcache() {
    // 删除游戏临时文件
    system("rm -rf /data/data/com.tencent.tmgp.sgame/files/ano_tmp");
    system("rm -rf /data/user/0/com.tencent.tmgp.sgame/files/ano_tmp");

    // 删除日志文件
    system("rm -rf storage/emulated/0/Android/data/com.tencent.tmgp.sgame/files/logs");

    // 删除缓存
    system("rm -rf storage/emulated/0/Android/data/com.tencent.tmgp.sgame/cache/");

    // 删除配置文件
    system("rm -rf /data/data/com.tencent.tmgp.sgame/shared_prefs/");

    printf("王者缓存已删除！\n");
}
```

### 10.2 地址有效性检测

```cpp
// 文件: 过缺页.h

bool isMemoryTrap(uintptr_t addr) {
    // 基础地址范围检查
    if (addr < 0x10000000 || addr > 0xFFFFFFFFFF || addr % 4 != 0) {
        return false;
    }

    static int pageSize = getpagesize();  // 通常是4096
    unsigned char vec = 0;
    unsigned long start = addr & (~(pageSize - 1));  // 页对齐

#if __aarch64__
    // ARM64架构使用mincore系统调用检查页面是否在内存中
    // ...
#elif __arm__
    // ARM32架构
    register int64_t r7 asm("r7") = SYS_mincore;
    register int64_t r0 asm("r0") = start;
    register int64_t r1 asm("r1") = pageSize;
    register int64_t r2 asm("r2") = (int64_t)&vec;

    asm volatile("swi #0"
        : "=r"(r0)
        : "r"(r7), "0"(r0), "r"(r1), "r"(r2)
        : "memory", "cc"
    );
#endif

    return true;
}
```

**页对齐计算**：
```cpp
unsigned long start = addr & (~(pageSize - 1));
// pageSize = 4096 = 0x1000
// pageSize - 1 = 0xFFF
// ~(pageSize - 1) = 0xFFFFF000
// addr & 0xFFFFF000 清除低12位，实现4KB对齐
```

**mincore系统调用**：
- 检查指定页面是否在物理内存中
- 用于避免访问未映射的内存导致崩溃

### 10.3 截图排除机制

```cpp
// 创建不被截图捕获的Surface
auto surfaceControl = surfaceComposerClient.CreateSurface(name, width, height, true);

// 在CreateSurface内部:
if (skipScreenshot && Functionals::GetInstance().systemVersion >= 12) {
    flags |= 0x40;  // 设置截图排除标志
}

// Android 10/11使用LayerMetadata
if (skipScreenshot && (systemVersion == 10 || systemVersion == 11)) {
    layerMetadata.setInt32(2u, 441731);  // 特殊标识
}
```

---

## 11. 关键C/C++语法详解

### 11.1 指针与引用

```cpp
// 指针：存储变量的内存地址
int x = 10;
int *p = &x;     // p指向x的地址
*p = 20;         // 通过指针修改x的值，现在x=20

// 引用：变量的别名
int &r = x;      // r是x的引用
r = 30;          // 现在x=30

// 指针的指针
int **pp = &p;   // pp指向p的地址
**pp = 40;       // 现在x=40
```

### 11.2 结构体与类

```cpp
// 结构体（默认public）
struct Vector3A {
    float X, Y, Z;
};

// 类（默认private）
class c_driver {
private:
    int fd;

public:
    void 初始化(pid_t 进程);
    bool 读取(uintptr_t 地址, void *buffer, size_t size);
};

// 使用
Vector3A pos = {1.0f, 2.0f, 3.0f};
c_driver driver;
driver.初始化(1234);
```

### 11.3 模板

```cpp
// 函数模板
template <typename T>
T 读取(uintptr_t 地址) {
    T result;
    // ... 读取逻辑
    return result;
}

// 使用
int i = 读取<int>(addr);
float f = 读取<float>(addr);
long l = 读取<long>(addr);

// 类模板
template <typename T>
struct StrongPointer {
    T *pointer;
    T* get() { return pointer; }
};

StrongPointer<Surface> sp;
```

### 11.4 内联汇编

```cpp
// GCC内联汇编语法
asm volatile(
    "指令"
    : 输出操作数
    : 输入操作数
    : 破坏列表
);

// 示例：ARM系统调用
register int64_t r7 asm("r7") = SYS_mincore;  // 系统调用号
register int64_t r0 asm("r0") = start;        // 参数1

asm volatile("swi #0"          // 软件中断指令
    : "=r"(r0)                 // 输出：r0
    : "r"(r7), "0"(r0)         // 输入：r7和r0
    : "memory", "cc"           // 可能修改内存和条件码
);
```

### 11.5 Lambda表达式

```cpp
// 基本语法
auto func = [捕获列表](参数列表) -> 返回类型 { 函数体 };

// 示例：屏幕方向检测线程
std::thread *orithread = new std::thread([&] {
    // [&] 表示按引用捕获所有外部变量
    while (true) {
        Orientation = atoi(exec("...").c_str());
        std::this_thread::sleep_for(0.5s);
    }
});

// 捕获方式
// [] - 不捕获
// [=] - 按值捕获所有
// [&] - 按引用捕获所有
// [x] - 按值捕获x
// [&x] - 按引用捕获x
// [=, &x] - 默认按值，x按引用
```

### 11.6 智能指针和RAII

```cpp
// unique_ptr - 独占所有权
std::unique_ptr<int> p1(new int(10));
// p1超出作用域时自动释放

// shared_ptr - 共享所有权
std::shared_ptr<int> p2 = std::make_shared<int>(20);
std::shared_ptr<int> p3 = p2;  // 引用计数+1

// RAII示例
class FileHandle {
    FILE *fp;
public:
    FileHandle(const char *path) { fp = fopen(path, "r"); }
    ~FileHandle() { if(fp) fclose(fp); }  // 析构时自动关闭
};
```

### 11.7 位运算

```cpp
// 按位与 &
int a = 0xFF00 & 0x0F0F;  // = 0x0F00

// 按位或 |
int b = 0xFF00 | 0x00FF;  // = 0xFFFF

// 按位异或 ^
int c = 0xFF00 ^ 0xFFFF;  // = 0x00FF

// 取反 ~
int d = ~0x0000FFFF;      // = 0xFFFF0000

// 左移 <<
int e = 1 << 4;           // = 16 (二进制 10000)

// 右移 >>
int f = 16 >> 2;          // = 4

// 实际应用：提取地址低40位
地址 & 0xFFFFFFFFFF
// 0xFFFFFFFFFF = 40个1，清除高位
```

---

## 附录A：常用偏移量表

| 偏移 | 数据类型 | 描述 | 所属结构 |
|------|----------|------|----------|
| 0xB939688 | 指针 | 矩阵链起点 | 基址头 |
| 0xBAC29C8 | 指针 | 对象数组链起点 | 基址头 |
| 0x420 | 指针 | 玩家信息组件 | 对象 |
| 0x3F8 | 指针 | 坐标组件链头 | 对象 |
| 0x324 | Vector3 | 世界坐标 | 坐标组件 |
| 0x68 | float | 当前血量 | 对象 |
| 0x40 | int | 对象类型 | 玩家信息 |
| 0x100 | int | 阵营ID | 玩家信息 |
| 0x428 | 指针 | 名字组件 | 对象 |
| 0x74 | int | 开镜状态 | 对象 |
| 0x310 | float[16] | 视图矩阵 | 矩阵对象 |

---

## 附录B：全局配置数组

```cpp
float NumIo[50];  // 数值配置
bool DrawIo[50];  // 开关配置

// NumIo索引说明
// [0] - 瞄准模式
// [3] - 瞄准范围
// [4] - 瞄准基础速度
// [5] - 初始触摸X
// [6] - 初始触摸Y
// [7] - 触摸范围
// [9] - 循环延迟(毫秒)
// [10] - 左右手模式
// [49] - 队伍选择

// DrawIo索引说明
// [1] - 射线开关
// [2] - 自瞄开关
// [3] - 方框开关
// [4] - 距离/名字开关
// [10] - ESP总开关
// [20] - 准心显示
// [21] - 触摸位置显示
```

---

## 附录C：推荐学习资源

### C/C++基础
- 《C Primer Plus》
- 《C++ Primer》
- cppreference.com

### Linux系统编程
- 《Linux系统编程》
- 《深入理解Linux内核》
- man pages (man 2 ioctl, man 2 syscall)

### Android NDK
- Android NDK官方文档
- 《Android C++高级编程》

### 图形编程
- OpenGL ES官方文档
- ImGui官方文档 (github.com/ocornut/imgui)

### 逆向工程
- 《逆向工程核心原理》
- IL2CPPDumper工具使用

---

> **文档版本**: 1.0
> **最后更新**: 2024年
> **适用项目**: AllTearGameCheatKernel 王者荣耀模块

---

## 12. 应用程序-内核-驱动调用逻辑详解

本章节深入详解从用户层应用程序到内核驱动的完整调用链路。

### 12.1 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                    用户层 (Ring 3)                           │
│  ┌─────────────────┐    ┌─────────────────────────────────┐ │
│  │   应用程序       │    │   libc库                        │ │
│  │   C++程序        │───▶│   ioctl/open/close              │ │
│  └─────────────────┘    └─────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼ 系统调用
┌─────────────────────────────────────────────────────────────┐
│                    内核层 (Ring 0)                           │
│  ┌─────────────────┐    ┌─────────────────────────────────┐ │
│  │  系统调用接口    │    │   内核驱动模块                   │ │
│  │  sys_ioctl      │───▶│   .ko文件                       │ │
│  └─────────────────┘    └─────────────────────────────────┘ │
│                              │                               │
│                              ▼                               │
│  ┌─────────────────────────────────────────────────────────┐│
│  │  内存管理: copy_to_user / copy_from_user                ││
│  │           access_process_vm                             ││
│  └─────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
```

### 12.2 关键数据结构

#### 12.2.1 IOCTL控制码定义

```c
#define OP_INIT_KEY    0x800   // 初始化密钥
#define OP_READ_MEM    0x801   // 读取内存
#define OP_WRITE_MEM   0x802   // 写入内存
#define OP_MODULE_BASE 0x803   // 获取模块基址
```

**控制码说明**：
- 控制码是用户层与内核驱动通信的"命令标识"
- 驱动根据控制码执行不同操作
- 0x800-0x803 是自定义的控制码范围

#### 12.2.2 通信数据结构

```c
// 内存拷贝结构体 - 用于读写内存
typedef struct _COPY_MEMORY {
    pid_t pid;           // 目标进程PID
    uintptr_t addr;      // 目标内存地址
    void* buffer;        // 用户空间缓冲区指针
    size_t size;         // 读写大小
} COPY_MEMORY, *PCOPY_MEMORY;

// 模块基址结构体 - 用于获取SO库基址
typedef struct _MODULE_BASE {
    pid_t pid;           // 目标进程PID
    char* name;          // 模块名称 (如 "libUE4.so")
    uintptr_t base;      // 返回的基址
} MODULE_BASE, *PMODULE_BASE;
```

### 12.3 详细调用流程

#### 12.3.1 时序图

```
┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐
│   App   │    │  LibC   │    │ Kernel  │    │ Driver  │    │ Target  │
└────┬────┘    └────┬────┘    └────┬────┘    └────┬────┘    └────┬────┘
     │              │              │              │              │
     │ 1. 初始化阶段                                              │
     │──open()─────▶│              │              │              │
     │              │──sys_open───▶│              │              │
     │              │              │──open回调───▶│              │
     │◀─────────────────────────────fd────────────│              │
     │              │              │              │              │
     │ 2. 设置目标进程                                            │
     │ initialize(pid)             │              │              │
     │ (本地保存PID)               │              │              │
     │              │              │              │              │
     │ 3. 读取内存                                                │
     │ 构造COPY_MEMORY             │              │              │
     │──ioctl()────▶│              │              │              │
     │              │──sys_ioctl──▶│              │              │
     │              │              │──ioctl回调──▶│              │
     │              │              │              │──读取内存───▶│
     │              │              │              │◀────数据─────│
     │              │              │◀─copy_to_user─│              │
     │◀─────────────────────数据────│              │              │
     │              │              │              │              │
     │ 4. 关闭设备                                                │
     │──close()────▶│              │              │              │
     │              │──sys_close──▶│              │              │
     │              │              │──release回调▶│              │
     │              │              │              │              │
```

#### 12.3.2 流程说明

**步骤1：初始化阶段**
```
App ──open("/dev/设备名", O_RDWR)──▶ LibC ──sys_open──▶ Kernel ──driver_open──▶ Driver
                                                                                    │
                                                              返回文件描述符fd ◀────┘
```

**步骤2：设置目标进程**
```
App: initialize(pid) → 保存目标PID到类成员变量
```

**步骤3：读取内存**
```
App ──构造COPY_MEMORY结构体──▶ ioctl(fd, OP_READ_MEM, &cm)
                                        │
                                        ▼
LibC ──sys_ioctl系统调用──▶ Kernel ──driver_ioctl回调──▶ Driver
                                                          │
                                                          ▼ 解析COPY_MEMORY
                                                    跨进程读取目标内存
                                                          │
                                                          ▼ copy_to_user
                                                    返回数据到用户空间
```

**步骤4：关闭设备**
```
App ──close(fd)──▶ LibC ──sys_close──▶ Kernel ──driver_release──▶ Driver
```

### 12.4 用户层代码详解

#### 12.4.1 驱动类完整实现

```cpp
#include <sys/fcntl.h>
#include <sys/ioctl.h>
#include <stdio.h>
#include <dirent.h>
#include <unistd.h>

class c_driver {
private:
    int fd;              // 驱动文件描述符
    pid_t pid;           // 目标进程PID

    // ========== 通信数据结构 ==========
    typedef struct _COPY_MEMORY {
        pid_t pid;       // 目标进程ID
        uintptr_t addr;  // 要读写的地址
        void* buffer;    // 数据缓冲区
        size_t size;     // 数据大小
    } COPY_MEMORY;

    typedef struct _MODULE_BASE {
        pid_t pid;       // 目标进程ID
        char* name;      // 模块名
        uintptr_t base;  // 模块基址(输出)
    } MODULE_BASE;

    // ========== IOCTL控制码 ==========
    enum OPERATIONS {
        OP_INIT_KEY    = 0x800,
        OP_READ_MEM    = 0x801,
        OP_WRITE_MEM   = 0x802,
        OP_MODULE_BASE = 0x803,
    };

    // ========== 查找驱动设备节点 ==========
    // 原理：驱动加载后会在/dev下创建字符设备文件
    char* getDriverPath() {
        DIR* devDir = opendir("/dev");
        struct dirent* entry;

        while ((entry = readdir(devDir))) {
            // DT_CHR表示字符设备
            if (entry->d_type == DT_CHR) {
                // 这里需要根据实际驱动名称匹配
                std::string device_name = "/dev/";
                device_name += entry->d_name;
                closedir(devDir);
                return strdup(device_name.c_str());
            }
        }
        closedir(devDir);
        return nullptr;
    }

public:
    // ========== 构造函数：打开驱动 ==========
    c_driver() : fd(-1), pid(0) {
        char* device_name = getDriverPath();

        // open()系统调用打开设备文件
        // O_RDWR = 可读可写模式
        fd = open(device_name, O_RDWR);

        if (fd == -1) {
            printf("无法打开驱动节点\n");
            exit(0);
        }
        printf("驱动节点打开成功: %s, fd=%d\n", device_name, fd);
        free(device_name);
    }

    // ========== 析构函数：关闭驱动 ==========
    ~c_driver() {
        if (fd > 0) {
            close(fd);  // 关闭文件描述符
        }
    }

    // ========== 初始化目标进程 ==========
    void initialize(pid_t target_pid) {
        this->pid = target_pid;
    }

    // ========== 核心：读取内存 ==========
    bool read(uintptr_t addr, void* buffer, size_t size) {
        // 地址有效性检查
        // 0x10000000 = 256MB，防止读取过低地址
        // 0x10000000000 = 64GB，防止读取过高地址
        if (addr <= 0x10000000 || addr % 4 != 0 || addr >= 0x10000000000) {
            return false;
        }

        // 构造通信结构体
        COPY_MEMORY cm;
        cm.pid = this->pid;           // 目标进程
        cm.addr = addr & 0xFFFFFFFFFF; // 地址掩码(40位)
        cm.buffer = buffer;            // 接收缓冲区
        cm.size = size;                // 读取大小

        // ioctl系统调用 - 核心通信方式
        // fd: 驱动文件描述符
        // OP_READ_MEM: 控制码，告诉驱动要读取内存
        // &cm: 传递给驱动的参数
        if (ioctl(fd, OP_READ_MEM, &cm) != 0) {
            return false;
        }
        return true;
    }

    // ========== 核心：写入内存 ==========
    bool write(uintptr_t addr, void* buffer, size_t size) {
        COPY_MEMORY cm;
        cm.pid = this->pid;
        cm.addr = addr;
        cm.buffer = buffer;
        cm.size = size;

        if (ioctl(fd, OP_WRITE_MEM, &cm) != 0) {
            return false;
        }
        return true;
    }

    // ========== 模板读取：支持任意类型 ==========
    template<typename T>
    T read(uintptr_t addr) {
        T result;
        if (this->read(addr, &result, sizeof(T))) {
            return result;
        }
        return T{};  // 失败返回默认值
    }

    // ========== 模板写入：支持任意类型 ==========
    template<typename T>
    bool write(uintptr_t addr, T value) {
        return this->write(addr, &value, sizeof(T));
    }

    // ========== 获取模块基址 ==========
    uintptr_t get_module_base(char* name) {
        MODULE_BASE mb;
        char buf[0x100];
        strcpy(buf, name);

        mb.pid = this->pid;
        mb.name = buf;

        if (ioctl(fd, OP_MODULE_BASE, &mb) != 0) {
            return 0;
        }
        return mb.base;
    }

    // ========== 便捷函数：读取指针 ==========
    long read_pointer(long addr) {
        if (addr <= 0x10000000 || addr % 4 != 0 || addr >= 0x10000000000) {
            return 0;
        }
        long result;
        if (this->read(addr, &result, sizeof(uintptr_t))) {
            return result;
        }
        return 0;
    }

    // ========== 便捷函数：读取浮点数 ==========
    float read_float(long addr) {
        float var;
        if (this->read(addr & 0xFFFFFFFFFF, &var, sizeof(var))) {
            return var;
        }
        return 0.0f;
    }

    // ========== 便捷函数：读取整数 ==========
    int read_int(long addr) {
        int data;
        if (this->read(addr & 0xFFFFFFFFFF, &data, sizeof(data))) {
            return data;
        }
        return 0;
    }
};

// 全局驱动实例
static c_driver* driver = new c_driver();
```

#### 12.4.2 获取进程PID

```cpp
// 通过包名获取进程PID
pid_t get_pid(const char* package_name) {
    int id = -1;
    DIR* dir;
    FILE* fp;
    char filename[64];
    char cmdline[64];
    struct dirent* entry;

    // 遍历/proc目录
    dir = opendir("/proc");
    while ((entry = readdir(dir)) != NULL) {
        id = atoi(entry->d_name);  // 目录名就是PID
        if (id != 0) {
            // 读取进程的命令行(包名)
            sprintf(filename, "/proc/%d/cmdline", id);
            fp = fopen(filename, "r");
            if (fp) {
                fgets(cmdline, sizeof(cmdline), fp);
                fclose(fp);
                // 比较包名
                if (strcmp(package_name, cmdline) == 0) {
                    closedir(dir);
                    return id;
                }
            }
        }
    }
    closedir(dir);
    return -1;
}
```

#### 12.4.3 获取模块基址(用户层实现)

```cpp
// 通过解析/proc/pid/maps获取模块基址
unsigned long get_module_base(int pid, const char* module_name) {
    FILE* fp;
    unsigned long addr = 0;
    char* pch;
    char filename[64];
    char line[1024];

    // maps文件包含进程的内存映射信息
    snprintf(filename, sizeof(filename), "/proc/%d/maps", pid);
    fp = fopen(filename, "r");

    if (fp != NULL) {
        while (fgets(line, sizeof(line), fp)) {
            // 查找包含模块名的行
            // 例如: 7a2b3c4d5000-7a2b3c4d6000 r-xp ... libUE4.so
            if (strstr(line, module_name)) {
                // 解析起始地址
                pch = strtok(line, "-");
                addr = strtoul(pch, NULL, 16);
                break;
            }
        }
        fclose(fp);
    }
    return addr;
}
```

### 12.5 内核驱动层原理

#### 12.5.1 驱动模块结构

```
┌─────────────────────────────────────────────────────────────┐
│                    内核驱动模块结构                           │
│  ┌─────────────────┐    ┌─────────────────────────────────┐ │
│  │  module_init    │    │   file_operations               │ │
│  │  驱动初始化      │───▶│   文件操作函数表                 │ │
│  └─────────────────┘    └─────────────────────────────────┘ │
│                              │                               │
│  ┌─────────────────┐         ├──▶ .open (打开设备)          │
│  │  module_exit    │         ├──▶ .release (关闭设备)       │
│  │  驱动卸载       │         └──▶ .unlocked_ioctl (核心)    │
│  └─────────────────┘                     │                   │
└──────────────────────────────────────────┼───────────────────┘
                                           ▼
                    ┌──────────────────────────────────────────┐
                    │           ioctl处理逻辑                   │
                    │  ┌─────────────────────────────────────┐ │
                    │  │  switch(控制码)                      │ │
                    │  │    case OP_READ_MEM:  读取内存处理   │ │
                    │  │    case OP_WRITE_MEM: 写入内存处理   │ │
                    │  │    case OP_MODULE_BASE: 获取基址处理 │ │
                    │  └─────────────────────────────────────┘ │
                    └──────────────────────────────────────────┘
```

#### 12.5.2 内核驱动伪代码

```c
// ========== 内核驱动框架伪代码 ==========

#include <linux/module.h>
#include <linux/kernel.h>
#include <linux/fs.h>
#include <linux/uaccess.h>
#include <linux/mm.h>
#include <linux/sched.h>

// 与用户层相同的结构体定义
struct copy_memory {
    pid_t pid;
    unsigned long addr;
    void __user *buffer;
    size_t size;
};

// ioctl控制码
#define OP_READ_MEM    0x801
#define OP_WRITE_MEM   0x802
#define OP_MODULE_BASE 0x803

// ========== 核心：跨进程内存读取 ==========
static int read_process_memory(pid_t pid, unsigned long addr,
                               void __user *buffer, size_t size) {
    struct task_struct *task;
    struct mm_struct *mm;
    void *kernel_buf;
    int ret = -1;

    // 1. 通过PID查找进程task_struct
    task = pid_task(find_vpid(pid), PIDTYPE_PID);
    if (!task) return -ESRCH;

    // 2. 获取进程的内存描述符
    mm = get_task_mm(task);
    if (!mm) return -EINVAL;

    // 3. 分配内核缓冲区
    kernel_buf = kmalloc(size, GFP_KERNEL);
    if (!kernel_buf) {
        mmput(mm);
        return -ENOMEM;
    }

    // 4. 关键：使用内核函数读取目标进程内存
    // access_process_vm是Linux内核提供的跨进程内存访问函数
    ret = access_process_vm(task, addr, kernel_buf, size, FOLL_FORCE);

    // 5. 将数据从内核空间拷贝到用户空间
    if (ret > 0) {
        if (copy_to_user(buffer, kernel_buf, size)) {
            ret = -EFAULT;
        }
    }

    kfree(kernel_buf);
    mmput(mm);
    return ret == size ? 0 : -EIO;
}

// ========== 核心：跨进程内存写入 ==========
static int write_process_memory(pid_t pid, unsigned long addr,
                                void __user *buffer, size_t size) {
    struct task_struct *task;
    void *kernel_buf;
    int ret = -1;

    task = pid_task(find_vpid(pid), PIDTYPE_PID);
    if (!task) return -ESRCH;

    kernel_buf = kmalloc(size, GFP_KERNEL);
    if (!kernel_buf) return -ENOMEM;

    // 1. 从用户空间拷贝数据到内核
    if (copy_from_user(kernel_buf, buffer, size)) {
        kfree(kernel_buf);
        return -EFAULT;
    }

    // 2. 写入目标进程内存
    // FOLL_WRITE标志表示写操作
    ret = access_process_vm(task, addr, kernel_buf, size,
                            FOLL_FORCE | FOLL_WRITE);

    kfree(kernel_buf);
    return ret == size ? 0 : -EIO;
}

// ========== ioctl入口函数 ==========
static long driver_ioctl(struct file *file, unsigned int cmd,
                         unsigned long arg) {
    struct copy_memory cm;

    switch (cmd) {
        case OP_READ_MEM:
            // 从用户空间获取参数
            if (copy_from_user(&cm, (void __user *)arg, sizeof(cm)))
                return -EFAULT;
            return read_process_memory(cm.pid, cm.addr,
                                       cm.buffer, cm.size);

        case OP_WRITE_MEM:
            if (copy_from_user(&cm, (void __user *)arg, sizeof(cm)))
                return -EFAULT;
            return write_process_memory(cm.pid, cm.addr,
                                        cm.buffer, cm.size);

        case OP_MODULE_BASE:
            // 获取模块基址逻辑...
            break;
    }
    return -EINVAL;
}

// ========== 文件操作结构体 ==========
static struct file_operations driver_fops = {
    .owner          = THIS_MODULE,
    .open           = driver_open,
    .release        = driver_release,
    .unlocked_ioctl = driver_ioctl,
};
```

### 12.6 完整调用流程图

```
┌───────────────────────────────────────────────────────────────┐
│                        使用示例流程                            │
├───────────────────────────────────────────────────────────────┤
│                                                               │
│   ┌─────────┐                                                 │
│   │  开始   │                                                 │
│   └────┬────┘                                                 │
│        │                                                      │
│        ▼                                                      │
│   ┌────────────────────────────────────┐                      │
│   │ 创建driver实例                      │                      │
│   │ driver = new c_driver()            │                      │
│   └────┬───────────────────────────────┘                      │
│        │                                                      │
│        ▼                                                      │
│   ┌────────────────────────────────────┐                      │
│   │ 获取游戏PID                         │                      │
│   │ pid = get_pid("com.tencent...")    │                      │
│   └────┬───────────────────────────────┘                      │
│        │                                                      │
│        ▼                                                      │
│   ┌────────────────────────────────────┐                      │
│   │ 初始化driver                        │                      │
│   │ driver->initialize(pid)            │                      │
│   └────┬───────────────────────────────┘                      │
│        │                                                      │
│        ▼                                                      │
│   ┌────────────────────────────────────┐                      │
│   │ 获取模块基址                        │                      │
│   │ base = get_module_base("libUE4.so")│                      │
│   └────┬───────────────────────────────┘                      │
│        │                                                      │
│        ▼                                                      │
│   ┌────────────────────────────────────┐                      │
│   │ 读取游戏数据                        │                      │
│   │ data = driver->read<long>(base+off)│                      │
│   └────┬───────────────────────────────┘                      │
│        │                                                      │
│        ▼                                                      │
│   ┌────────────────────────────────────┐                      │
│   │ 处理数据                            │                      │
│   └────┬───────────────────────────────┘                      │
│        │                                                      │
│        ▼                                                      │
│   ┌─────────┐                                                 │
│   │  结束   │                                                 │
│   └─────────┘                                                 │
│                                                               │
└───────────────────────────────────────────────────────────────┘
```

### 12.7 使用示例

```cpp
int main() {
    // 1. 创建驱动实例(构造函数会自动打开驱动)
    c_driver* driver = new c_driver();

    // 2. 获取王者荣耀进程PID
    pid_t game_pid = get_pid("com.tencent.tmgp.sgame");
    if (game_pid == -1) {
        printf("游戏未运行\n");
        return -1;
    }
    printf("游戏PID: %d\n", game_pid);

    // 3. 初始化驱动(绑定目标进程)
    driver->initialize(game_pid);

    // 4. 获取libUE4.so基址
    uintptr_t base = driver->get_module_base("libUE4.so");
    printf("libUE4.so基址: 0x%lx\n", base);

    // 5. 读取游戏数据示例
    // 假设 base + 0x1234567 是某个重要数据的偏移
    long some_pointer = driver->read<long>(base + 0x1234567);
    printf("读取到指针: 0x%lx\n", some_pointer);

    // 6. 链式读取(指针链)
    long level1 = driver->read<long>(some_pointer + 0x10);
    long level2 = driver->read<long>(level1 + 0x20);
    float health = driver->read<float>(level2 + 0x30);
    printf("血量: %.2f\n", health);

    // 7. 清理
    delete driver;
    return 0;
}
```

### 12.8 两种内存读取方式对比

| 特性 | 内核驱动方式 (ioctl) | syscall方式 (process_vm_readv) |
|------|---------------------|-------------------------------|
| 需要ROOT | 是 | 是 |
| 需要加载驱动 | 是 | 否 |
| 检测难度 | 较难检测 | 容易被检测 |
| 实现复杂度 | 复杂(需内核开发) | 简单 |
| 性能 | 较高 | 一般 |
| 稳定性 | 取决于驱动质量 | 系统原生支持 |

#### syscall方式代码示例

```c
// 不需要驱动，直接使用系统调用
#include <sys/uio.h>
#include <sys/syscall.h>

// ARM64的系统调用号
#define __NR_process_vm_readv  270
#define __NR_process_vm_writev 271

bool read_memory(pid_t pid, void* addr, void* buffer, size_t size) {
    struct iovec local[1];
    struct iovec remote[1];

    local[0].iov_base = buffer;    // 本地缓冲区
    local[0].iov_len = size;
    remote[0].iov_base = addr;     // 远程地址
    remote[0].iov_len = size;

    // 直接系统调用
    ssize_t ret = syscall(__NR_process_vm_readv,
                          pid, local, 1, remote, 1, 0);
    return ret == size;
}
```

### 12.9 总结：完整调用链路

```
应用程序调用 driver->read(addr, buffer, size)
    ↓
构造 COPY_MEMORY 结构体
    ↓
调用 ioctl(fd, OP_READ_MEM, &cm)
    ↓
触发系统调用 sys_ioctl
    ↓
内核分发到驱动的 unlocked_ioctl 函数
    ↓
驱动解析参数，调用 access_process_vm
    ↓
内核读取目标进程内存
    ↓
copy_to_user 将数据返回用户空间
    ↓
应用程序获得读取结果
```

这就是完整的应用程序-内核-驱动调用逻辑关系。通过理解这个调用链，可以更好地理解Linux内核驱动的工作原理以及用户态与内核态之间的通信机制。
